# Placeholder

Phase 4 will populate this directory with the REST API project (ASP.NET
Core) that exposes the Gold views as tool-callable endpoints. Keeping
the path in version control so the structure is stable.
